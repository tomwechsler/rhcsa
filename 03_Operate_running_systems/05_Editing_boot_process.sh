#Working on the rhel system in virtualbox

#Boot Process
#1 - BIOS/UEFI => The system bios/uefi is used to start the system and locate the boot partition
#2 - GRUB => The boot loader in RHEL is GRUB and allows you to selevt the Linux kernel to load
#3 - Kernel => The kernel is loaded but prior to this the initialization ram dis is loaded to customize the boot process to your hardware

#Shutdown the redhat system and => Start in "Normal Start"

#Hit quick the arrow keys to stop the countdown

#Navigate to the linux entry line and edit with the following (next video)